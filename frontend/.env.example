# URL prefix for all API calls (proxied to backend)
NEXT_PUBLIC_API_URL=/api

# Backend URL — change this if your backend runs elsewhere
# For Docker: use http://schism-api:8000
SCHISM_API_PROXY_TARGET=http://localhost:8000
